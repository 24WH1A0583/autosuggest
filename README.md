# autosuggest

Verifying my git set-up : )
Hi!!
changes flow for pushing into github (UI) :
1. add(plus button)(after saving)
2. commit with message
3. push/ sync

commands(CLI in terminal of VScode):
git add -A(stages all files not just one file)
git commit -m "some message"(eg: git commit -m "4th commit")
git push
